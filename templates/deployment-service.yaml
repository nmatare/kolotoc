apiVersion: v1
kind: Service
metadata:
  name: {{ template "kolotoc.fullname" . }}-scheduler
  labels:
    app: {{ template "kolotoc.name" . }}
    chart: {{ template "kolotoc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Deployment }}
spec:
  clusterIP: None
  ports:
  - name: ssh 
    port: {{ .Values.ssh.port }}
    targetPort: {{ .Values.ssh.port }}
  selector:
    app: {{ template "kolotoc.name" . }}
    release: {{ .Release.Name }}
    role: scheduler
